Version 1.

Esta version despues de hacer login muestra el contenido de la camara en el navegador, no muestra el menu.
Esto es debido a que devuelve
def index():
    return Response(generar_frames(),
                    mimetype='multipart/x-mixed-replace; boundary=frame')
El problema de esta versión es que al cerrar el navegador, la camara sigue activa.

Version 2.

Se utiliza una clase para la logica de la camara
Al cerrar el navegador, la camara sigue activa.